namespace CompatibleSoftware.Poker.Domain
{
    /// <summary>
    /// An enum of Card Suits 
    /// In some cases suits can make a difference so they are also ranked via an int
    /// </summary>
    public enum Suit
    {
        Clubs = 1,
        Diamonds = 2,
        Hearts = 3,
        Spades = 4
    }
}